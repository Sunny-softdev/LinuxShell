1. To identify number of cores in a system:

cat /proc/cpuinfo | grep cores

2. To open text file with gedit editor

gedit filename.txt

3. To identfy free space in the memory
free -h

4. Copy all the content from one folder to another:

cd Destination_folder

cp -r ../Source_folder/*.
